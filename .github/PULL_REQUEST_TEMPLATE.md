<!--
Thanks for opening a PR against dockerize2.

Reminders:
- Branch from `master`; target `master`. No stacking.
- Conventional Commits in the squash-merge title (e.g. `feat:`, `fix:`,
  `chore:`, `ci:`, `docs:`). Release Please uses these to bump versions
  and write CHANGELOG entries.
- Add a `[Unreleased]` entry to CHANGELOG.md.
- SSH-signed commits required (GitHub must show "Verified").
- Run locally before pushing:
    uv run ruff check && uv run ruff format --check
    uv run mypy
    uv run pytest
-->

## Summary

<!-- 1–3 sentences: what changes and why. -->

## Test plan

- [ ] CI green on push
- [ ] (add any manual verification steps here)
