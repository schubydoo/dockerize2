# Every file in this repo is owned by the maintainer. Combined with
# branch protection's `require_code_owner_reviews: true`, this means every
# PR needs Schuby's explicit approval before merge.
* @schubydoo
